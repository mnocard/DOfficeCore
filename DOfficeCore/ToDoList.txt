добавить статус бар

значок обработки файла

асинхронная операция работы с извлечением строк из документов

асинхронный поиск и рнадомный дневник

Тесты для асинхронных методов

добавить в LineEditorService формат doc
тесты для doc

переписать логгер по нормальному. логгер переделать, чтобы сохранял текст в архиве(xml или json или zip)

добавить логгер в LineEditorService

Solved:
Заменить дерево на коллекцию, в которой каждый элемент будет содержать и диагноз, и раздел???
EditItem метод передавать CurrentItem и возможно полностью заменить все CurrentDiagnosis, CurrentBlock и CurrentLine на CurrentItem;
поэксперементировать с линком в тестовом проекте
переделать удаление элемента в три метода (удаление диагноза, удаление раздела и удаление строки)
переделать foreach в LINQ во всем ViewCollectionProvider
EditBlock будет переименовыывать все блоки с одинаковыми называниями...
не получается сделать рандомный дневник, потому что не выходит передать выбранную в датагриде секцию, из которой нужно получить диагноз
Дальше продолжать невозможно без рефакторинга...
добавить текст враппинг
добавить в заголовок датагридов название диагноза и блока
изменить List на list (потому что всё равно толку ноль - изменение элемента выдает ошибку при отображении данных, 
так как HashCode не меняется, а объект меняется. На данный момент от этой ошибки помогло то, что я убрал переопределенный метод
GetHashCode из модели данных, а без него смысла в List нет никакого)

